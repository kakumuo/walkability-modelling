{
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Run react-scripts",
            "runtimeExecutable": "react-scripts",
            "cwd": "${workspaceFolder}",
            "args": ["start"]
        }
    ]
}